package vartas.discord;

component grammar Entity extends de.monticore.literals.MCCommonLiterals{
    interface DiscordEntity = id:NatLiteral;

    User implements DiscordEntity =
        "<" "@" semicolon:["!"]? id:NatLiteral ">"
    ;

    TextChannel implements DiscordEntity =
        "<" "#" id:NatLiteral ">"
    ;

    Role implements DiscordEntity =
        "<" "@" "&" id:NatLiteral ">"
    ;
}