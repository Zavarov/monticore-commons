package vartas.monticore.cd2code.types;

component grammar CD2CodeBasicTypes extends de.monticore.types.MCBasicTypes {
    MCURLType implements MCObjectType <200> =
        {next("URL")}? Name
    ;

    MCInstantType implements MCObjectType <200> =
        {next("Instant")}? Name
    ;
}