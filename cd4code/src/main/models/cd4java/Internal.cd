
/**
 * @since 2.1
 */
public classdiagram Internal{
    /**
     * The default pattern used by the decorator if no matching class diagram is found
     * for the respective attribute.
     * This class only provides a simple getter and setter for the internal attribute.
     */
    public class Decorator <<T>>{
        public T get();
        public void set(T value);
    }
}