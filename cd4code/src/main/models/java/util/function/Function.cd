package java.util.function;

/**
 * @version Java 11
 */
public classdiagram Function{
    public interface Function <<T, R>> {
        //<V> Function<T,V> andThen(Function<? super R, ? extends V> after);
        R apply(T t);
        //<V> Function<V,R> compose(Function<?`super V, ? extends T> before);
    }
}